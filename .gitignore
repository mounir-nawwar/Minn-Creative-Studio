node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Firebase config files (contain API keys — use env vars instead)
firebase-applet-config.json
*firebase*config*.json

# Scripts with embedded service account keys
migrate.mjs
fix-uids.mjs

# Temp deploy files
*.yaml
key.json
cors.json

# SQLite database files
*.db
*.db-shm
*.db-wal

# Local file storage
storage/

# Personal local notes (never committed)
*.local.md
